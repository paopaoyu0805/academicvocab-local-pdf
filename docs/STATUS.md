# 项目状态

当前阶段：阶段 5：Android PWA 本地 PDF 选词与公开部署技术验证

状态：已完成

会话状态：可安全暂停；恢复前先阅读 `docs\HANDOFF.md`

最后通过检查：Android PWA 构建成功；阶段 2B、阶段 3、阶段 4 和阶段 5 自动测试共 42 项通过；GitHub Pages 公开版本已部署并通过手机 Wi-Fi 与移动网络人工访问验证；PDF 与候选内容只在浏览器内存中读取，未上传或保存。

本阶段完成内容：新增 Android 优先的 PWA 技术验证，可在夸克浏览器中本地打开 PDF、翻页并轻点单词；支持完整标题候选、普通换行句、跨行断词（如 `inter-` + `national`）、跨页句子、同词多次出现时按点中行定位，以及无标点片段与新页块隔离。公开测试地址为 `https://paopaoyu0805.github.io/academicvocab-local-pdf/`。PDF 不上传、不保存，刷新后清空；当前仍是选词与候选验证界面，不保存正式生词。

下一允许阶段：阶段 6：英语词形归一化技术验证。范围仅限把选中的屈折形式规范为原形（例如 `contains` → `contain`、复数和常见时态归一），保留原文形式与例句；不接入翻译、云同步、正式持久化或 Zotero 保存。

未解决问题：夸克/PDF.js 的原生拖选不可靠，当前单词操作采用轻点取词；复杂多栏、特殊字体和扫描 PDF 仍可能需要额外阅读顺序或 OCR 方案。实际本地持久化与正式 Zotero 保存、复习调度、翻译、云端同步和正式插件交互均未开始。
