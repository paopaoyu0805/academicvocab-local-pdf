# 项目状态

当前阶段：阶段 6：英语词形归一化与手机端响应性能验证

状态：进行中；等待优化版手机实测确认

会话状态：可安全暂停；恢复前先阅读 `docs\HANDOFF.md`

最后通过检查：Android PWA 生产构建成功；阶段 2B、阶段 3、阶段 4、阶段 5 和阶段 6 自动测试共 45 项通过；拆包后的本地首屏可正常启动且无浏览器错误。GitHub Pages 上一版已通过手机 Wi-Fi 与移动网络人工访问验证；本次性能优化版待发布后手机确认。PDF 与候选内容仍只在浏览器内存中读取，未上传或保存。

本阶段已完成内容：选中的英语屈折形式可在本地归一为原形，并保留歧义提示；候选弹窗先显示再异步完成例句处理。为避免夸克首次解析 1.7 MB 词形包失败，已改为入口内置的小型保守规则和常见不规则词表，原形处理不再需要额外词典下载。性能优化把首屏 JavaScript 从约 490 KB 降至约 14.9 KB、首屏 CSS 从约 233 KB 降至约 1.6 KB；PDF 引擎在首屏首次绘制后立即预热，相邻页文本在 PDF 打开后空闲预热；手机 PDF 渲染像素倍率设上限；点词优先使用浏览器原生光标定位；GitHub Pages 子路径下的 Service Worker 注册地址和缓存版本已修正。

本阶段剩余：发布性能优化版并由用户在夸克浏览器中确认网址首屏、PDF 首开、点词和候选显示速度；确认前不进入下一阶段。

未解决问题：首次访问仍需通过移动网络下载 PDF 引擎和 Worker，重复访问会由浏览器缓存提速；轻量词形规则对未覆盖的不规则词可能保守地保留原词，后续根据真实论文样本补充小型例外表，不再恢复大型运行时词典；夸克/PDF.js 的原生拖选不可靠，当前单词操作采用轻点取词；复杂多栏、特殊字体和扫描 PDF 仍可能需要额外阅读顺序或 OCR 方案。实际本地持久化与正式 Zotero 保存、复习调度、翻译、云端同步和正式插件交互均未开始。
