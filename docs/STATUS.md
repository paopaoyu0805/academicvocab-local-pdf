# 项目状态

当前阶段：阶段 7：Zotero 原形主词与原文下划线技术验证

状态：实现与构建已完成；等待在隔离 Zotero 开发环境手动安装并实测

会话状态：可安全暂停；恢复前先阅读 `docs\HANDOFF.md`

最后通过检查：自动测试共 49 项通过；Zotero 0.4.0 的 JavaScript 语法、禁止联网、禁止数据库直写、标记所有权、原文词形字段和原形字段检查全部通过。已构建 `D:\AcademicVocab\zotero-dev\builds\academicvocab-selection-poc-0.4.0.xpi`，SHA-256 为 `6EE4DBDC40ACFFB716B779D46BFA27F1467D5D1A6FEF04157B18AB3E55689657`。

本阶段已完成内容：Zotero 测试面板明确区分“选中的原文词形”和“拟录入主词（原形）”；候选例句切换后会重新根据上下文判断原形。紫色下划线仍保存原文文字和精确 PDF 坐标。本地捕获数据模型以显式原形作为主词键，可把 `contains`、`contained` 等原文词形合并到同一个 `contain`，同时在每条例句中保留实际原文词形。Android 与 Zotero 的归一化结果已有一致性测试。

本阶段剩余：用户在隔离 Zotero 开发环境手动安装 0.4.0 XPI，并确认面板原文词形、拟录入原形以及原文下划线均正确。当前测试插件仍不正式持久化生词，不得把“拟录入”表述为已经保存。

未解决问题：Zotero 正式生词持久化尚未启用；当前仅有存储无关的数据模型和隔离测试下划线。轻量词形规则对未覆盖的不规则词可能保守地保留原词，后续根据真实论文样本补充小型例外表。复习调度、翻译、云端同步和正式插件交互均未开始。
